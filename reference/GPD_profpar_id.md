# GPD profile parameter vector id

GPD profile parameter vector id

## Usage

``` r
GPD_profpar_id(
  parameter = c("shape", "scale", "quantile", "endpoint"),
  nbsca = 1,
  nbsha = 1,
  subparam_id = 0
)
```

## Arguments

- parameter:

  .

- nbsca:

  .

- nbsha:

  .

- subparam_id:

  .

## Value

The id of the desired GPD parameter in the specified internal vector
format.
