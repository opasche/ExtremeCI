% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GEV_profile_CIs.R
\name{GEV_profile_loglik}
\alias{GEV_profile_loglik}
\title{GEV profile log-likelihood}
\usage{
GEV_profile_loglik(
  val,
  Z,
  parameter = c("shape", "location", "scale", "return_level", "endpoint"),
  subparam_id = 0,
  return_period = 100,
  orthogonal = FALSE,
  X = NULL,
  x_rlvl = NULL,
  loc_cols = NULL,
  scale_cols = NULL,
  shape_cols = NULL,
  init = NULL,
  hessian = TRUE,
  maxit = 1e+06,
  method = c("Nelder-Mead", "BFGS", "CG", "L-BFGS-B", "SANN", "Brent"),
  ...
)
}
\arguments{
\item{val}{Parameter value at which to evaluate the GEV profile log-likelihood.}

\item{Z}{Block maxima observations.}

\item{parameter}{Parameter for which to compute the profile log-likelihood.}

\item{subparam_id}{Index of the parameter coefficient for which to compute the profile log-likelihood (for conditional/non-stationary fits).}

\item{return_period}{Return period for the \code{'return_level'} parameter.}

\item{orthogonal}{DEPRECATED.}

\item{X}{Covariate matrix (for conditional/non-stationary fits). Columns should be variables, and rows should be observations matching \code{Y}.}

\item{x_rlvl}{Covariate vector at which to reparametrize for the \code{'return_level'} or \code{'endpoint'} parametrizations (for conditional/non-stationary fits).}

\item{loc_cols}{Column indices of \code{X} to use as covariate for the (conditional) location parameter (for conditional/non-stationary fits).}

\item{scale_cols}{Column indices of \code{X} to use as covariate for the (conditional) scale parameter (for conditional/non-stationary fits).}

\item{shape_cols}{Column indices of \code{X} to use as covariate for the (conditional) shape parameter (for conditional/non-stationary fits).}

\item{init}{Optional initial values for the remaining parameter's optimisation process, in the correct internal format.}

\item{hessian}{Logical. Should a numerically differentiated Hessian matrix be returned? See \code{\link[stats:optim]{stats::optim()}} for more details.}

\item{maxit}{The maximum number of iterations. See \code{\link[stats:optim]{stats::optim()}} for more details.}

\item{method}{The optimisation method to be used. See \code{\link[stats:optim]{stats::optim()}} for more details.}

\item{...}{Other arguments passed to the \code{control} argument of \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
The GEV profile log-likelihood of \code{parameter} evaluated at \code{val}, given the data,
as a \code{GEV_profML} object containing:
\item{param_val}{(Named) parameter value at which the GEV profile log-likelihood was evaluated.}
\item{param_name}{Name of the evaluated profile likelihood parameter.}
\item{mle_other}{Maximum-likelihood estimate of the other GEV parameters.}
\item{loglik}{Profile GEV log-likelihood value of \code{parameter} evaluated at \code{val}, given the data.}
\item{conv}{Whether the optimisation procedure converged.
See the \code{convergence} output of \code{\link[stats:optim]{stats::optim()}} for more details.}
\item{hessian}{The loglikelihood hessian evaluated at the estimated parameters, given the data.}
\item{parameter}{Name of the evaluated profile likelihood parameter given as argument (redundent).}
\item{parametrization}{Likelihood parametrization.}
\item{subparam_id}{Index of the parameter coefficient for which the profile log-likelihood was computed.}
\item{id_param}{Index of the likelihood profile parameter, in the internal parameter vector format.}
}
\description{
GEV profile log-likelihood
}
