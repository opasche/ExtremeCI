% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GEV_profile_CIs.R
\name{GEV_profile_loglik_curve}
\alias{GEV_profile_loglik_curve}
\title{GEV profile log-likelihood curve}
\usage{
GEV_profile_loglik_curve(
  Z,
  parameter = c("shape", "location", "scale", "return_level", "endpoint"),
  subparam_id = 0,
  alpha = 0.05,
  return_period = 100,
  orthogonal = FALSE,
  X = NULL,
  x_rlvl = NULL,
  loc_cols = NULL,
  scale_cols = NULL,
  shape_cols = NULL,
  warmstart_table = NULL,
  stepsize = 0.1,
  steps_beyond_conf = 5,
  initial_MLE_para = c("classical", "same"),
  max_steps = 10000,
  hessian = TRUE,
  maxit = 1e+06,
  method = c("Nelder-Mead", "BFGS", "CG", "L-BFGS-B", "SANN", "Brent"),
  ...
)
}
\arguments{
\item{Z}{Block maxima observations.}

\item{parameter}{Parameter for which to compute the profile log-likelihood.}

\item{subparam_id}{Index of the parameter coefficient for which to compute the profile log-likelihood (for conditional/non-stationary fits).}

\item{alpha}{Confidence alpha for the profile likelihood confidence interval (i.e. for the confidence line on the profile plot).}

\item{return_period}{Return period for the \code{'return_level'} parameter.}

\item{orthogonal}{DEPRECATED.}

\item{X}{Covariate matrix (for conditional/non-stationary fits). Columns should be variables, and rows should be observations matching \code{Y}.}

\item{x_rlvl}{Covariate vector at which to reparametrize for the \code{'return_level'} or \code{'endpoint'} parametrizations (for conditional/non-stationary fits).}

\item{loc_cols}{Column indices of \code{X} to use as covariate for the (conditional) location parameter (for conditional/non-stationary fits).}

\item{scale_cols}{Column indices of \code{X} to use as covariate for the (conditional) scale parameter (for conditional/non-stationary fits).}

\item{shape_cols}{Column indices of \code{X} to use as covariate for the (conditional) shape parameter (for conditional/non-stationary fits).}

\item{warmstart_table}{Evaluation table from a previous run.}

\item{stepsize}{Numerical size of each evaluation step, in the profile parameter's scale.}

\item{steps_beyond_conf}{Number of additional steps to take (in each direction)
after the profile log-likelihood values reach below the confidence line.}

\item{initial_MLE_para}{Parametrization used for the initial maximum likelihood estimate (defaults to classical, for better stability).}

\item{max_steps}{Maximum number of steps taken (in each direction).
If the confidence line was not reached, the corresponding confidence interval endpoint will be infinite.}

\item{hessian}{Logical. Should a numerically differentiated Hessian matrix be returned? See \code{\link[stats:optim]{stats::optim()}} for more details.}

\item{maxit}{The maximum number of iterations. See \code{\link[stats:optim]{stats::optim()}} for more details.}

\item{method}{The optimisation method to be used. See \code{\link[stats:optim]{stats::optim()}} for more details.}

\item{...}{Other arguments passed to the \code{control} argument of \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
The GEV profile log-likelihood curve for the desired \code{parameter},
with confidence line and resulting \code{(1-alpha)} confidence interval, as a \code{GEV_profileLogLik} object containing:
\item{mle}{The estimated maximum likelihood GEV parameters, as a named vector
(expressed in the profile parametrization).}
\item{ci}{Length-two vector containing the lower and upper endpoints of the
desired profile likelihood confidence interval.}
\item{profile_loglik}{Named matrix containing the profile loglikelihood value (Column 2)
for each considered profile parameter value (Column 1).}
\item{conf_line}{Confidence line for the desired profile likelihood confidence interval.
See e.g. Coles (2001) for more details.}
\item{eval_table}{Tibble (\code{\link[tibble:tibble]{tibble::tibble()}}) containing the history of
profile log-likelihood evaluation values, and related metadata.}
\item{param_name}{Name of the profiled parameter (infered, for debugging purposes).}
\item{parameter}{Name of the profiled parameter (given).}
\item{parametrization}{Parametrization used for the profile likelihood.}
\item{subparam_id}{Index of the parameter coefficient for which the profile log-likelihood was computed.}
\item{id_param}{Index of the profile parameter in the GEV parameter vector.}
}
\description{
GEV profile log-likelihood curve
}
\references{
Coles, S. (2001). \emph{An Introduction to Statistical Modeling of Extreme Values}. Springer. \doi{doi:10.1007/978-1-4471-3675-0}.
}
