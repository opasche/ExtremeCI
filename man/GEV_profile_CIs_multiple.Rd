% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GEV_profile_CIs.R
\name{GEV_profile_CIs_multiple}
\alias{GEV_profile_CIs_multiple}
\title{Multi-value conditional GEV profile likelihood confidence intervals}
\usage{
GEV_profile_CIs_multiple(
  Z,
  parameter = c("return_level", "endpoint"),
  alpha = 0.05,
  return_period = 100,
  orthogonal = FALSE,
  X = NULL,
  X_rlvl = NULL,
  loc_cols = NULL,
  scale_cols = NULL,
  shape_cols = NULL,
  init_step_pos = 100,
  init_step_neg = 10,
  tol = 0.01,
  steps_beyond_conf = 5,
  initial_MLE_para = c("classical", "same"),
  max_steps = 10000,
  hessian = TRUE,
  maxit = 1e+06,
  method = c("Nelder-Mead", "BFGS", "CG", "L-BFGS-B", "SANN", "Brent"),
  parallel_strat = c("none", "multisession", "sequential", "multicore"),
  n_workers = NULL,
  ...
)
}
\arguments{
\item{Z}{Block maxima observations.}

\item{parameter}{Parameter for which to compute the profile likelihood confidence intervals.}

\item{alpha}{Confidence alpha for the profile likelihood confidence intervals.}

\item{return_period}{Return period for the \code{'return_level'} parameter.}

\item{orthogonal}{DEPRECATED.}

\item{X}{Covariate matrix (for conditional/non-stationary fits). Columns should be variables, and rows should be observations matching \code{Y}.}

\item{X_rlvl}{Covariate matrix at which to reparametrize for the \code{'return_level'} or \code{'endpoint'} parametrizations (for conditional/non-stationary fits).
Columns should be variables, and each row should give one covariate realization at which to reparametrize and obtain a CI.}

\item{loc_cols}{Column indices of \code{X} to use as covariate for the (conditional) location parameter (for conditional/non-stationary fits).}

\item{scale_cols}{Column indices of \code{X} to use as covariate for the (conditional) scale parameter (for conditional/non-stationary fits).}

\item{shape_cols}{Column indices of \code{X} to use as covariate for the (conditional) shape parameter (for conditional/non-stationary fits).}

\item{init_step_pos}{Initial numerical size of each evaluation step to the right, in the profile parameter's scale.}

\item{init_step_neg}{Initial numerical size of each evaluation step to the left, in the profile parameter's scale.}

\item{tol}{Numerical tolerance for convergence, in the profile parameter's scale.}

\item{steps_beyond_conf}{Number of additional steps to take (in each direction)
after the profile log-likelihood values reach below the confidence line.}

\item{initial_MLE_para}{Parametrization used for the initial maximum likelihood estimate (defaults to classical, for better stability).}

\item{max_steps}{Maximum number of steps taken (in each direction).
If the confidence line was not reached, the corresponding confidence interval endpoint will be infinite.}

\item{hessian}{Logical. Should a numerically differentiated Hessian matrix be returned? See \code{\link[stats:optim]{stats::optim()}} for more details.}

\item{maxit}{The maximum number of iterations. See \code{\link[stats:optim]{stats::optim()}} for more details.}

\item{method}{The optimisation method to be used. See \code{\link[stats:optim]{stats::optim()}} for more details.}

\item{parallel_strat}{Parallel strategy. One of \code{"sequential"} (default), \code{"multisession"}, \code{"multicore"}, or \code{"mixed"}.}

\item{n_workers}{A positive numeric scalar or a function specifying the maximum number of parallel futures
that can be active at the same time before blocking.
If a function, it is called without arguments when the future is created and its value is used to configure the workers.
The function should return a numeric scalar.
Defaults to \code{\link[future:re-exports]{future::availableCores()}}\code{-1} if \code{NULL} (default), with \code{"multicore"} constraint in the relevant case.
Ignored if \code{strategy=="sequential"}.}

\item{...}{Other arguments passed to the \code{control} argument of \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
The GEV profile log-likelihood \code{(1-alpha)} confidence intervals for the desired \code{parameter},
for each desired covariate values, as a \code{\link[tibble:tibble]{tibble::tibble()}}, with columns:
\item{obs}{Index of the observation (i.e. row) of X_rlvl for which the parameter estimate and CI was computed.}
\item{\verb{<parameter name>}}{Conditional estimate of the \code{parameter}.}
\item{ci_down}{Lower endpoint of the conditional \code{(1-alpha)} profile-likelihood confidence interval for \code{parameter}.}
\item{ci_up}{Upper endpoint of the conditional \code{(1-alpha)} profile-likelihood confidence interval for \code{parameter}.}
\item{parameter}{Name of the parameter for which the estimates and CIs were computed.}
\item{alpha}{Confidence alpha for the profile likelihood confidence intervals.}
\item{return_period}{Return period for the \code{'return_level'} parameter (only if \code{parameter==return_level}).}
}
\description{
For non-stationary models, the return level reparametrization depends on covariate values.
This function repeats the profile likelihood procedure for several covariate values.
It enables obtaining a return-level (or endpoint) curve, with profile-likelihood confidence bands,
as a function of the covariate values.
}
